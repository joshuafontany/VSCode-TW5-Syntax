# SYNTAX TEST "text.html.tiddlywiki5" "Anything between a tag name and its close must parse as an attribute"

# html.js takes attributes in a loop and stops at the first one parseAttribute declines. It then
# demands a closing slash or a greater-than at that position, and returns null when it finds
# neither — so a single stretch of text that parses as no attribute refuses the WHOLE tag, and
# TiddlyWiki yields plain text for all of it.
#
# Most junk escapes this, because parseAttribute takes so much: reAttributeName reads
# ([^\/\s>"'`=:]+) and an attribute needs no value, so <$link ###> binds ### as a valueless
# attribute and the tag stands. Three shapes measured against the parser do not escape it:
#
#   <$link to=<<>>>       a << with a >> ahead that names no macro. parseutils.js returns null
#                         deliberately here, its comment naming the intent: the enclosing tag
#                         fails rather than silently binding the attribute to true and reading the
#                         remainder as further attributes. Restores v5.3.8 behaviour.
#   <$link a/b="x">       a slash stands outside the attribute name class, and it reads as the
#                         self-closing slash instead, after which b="x" satisfies no closer.
#   <$link to: "x">       to: and a quote. The colon carries no assignment — TiddlyWiki binds
#                         valueless attributes here, and <$text text: word/> binds two of them —
#                         so the quote opens nothing the loop can take.
#
# A begin pattern CAN express this law. An attribute list carries alternation, not recursion, so
# a lookahead over the value forms parseutils.js declares decides the same question, and it costs
# nothing to run: forty attributes with no close return in under a millisecond, falling rather
# than rising with length.
#
# Nesting costs it a little: a macro value carrying a macro, a filter value carrying a
# transclusion. Oniguruma takes recursive subexpressions, so a guard can follow most of that, and
# hand-writing the filter grammar to follow the rest reads WORSE than a permissive pattern does.
#
# Cut ground decides the matter. `npm run attribute-guard --cut` shortens each tag at a seeded
# offset — what an author holds mid-keystroke — and there the guard refuses several times more
# constructs that TiddlyWiki BUILDS than it does on whole tags. A tag cut mid-macro,
# `<$text text=<<caption>`, closes for the parser and not for the guard: TiddlyWiki recovers where
# a guard demanding a complete value cannot. A grammar that reads worse the more unfinished its
# input stands against the gradient it exists to colour. `npm run attribute-guard` takes both the
# guard and the parser to every tag in TiddlyWiki's own tiddlers and reports the split. The guard
# refuses a handful of tags the parser BUILDS, every one of them nested, and each would stop
# colouring; it admits a handful the parser refuses, which the grammar already does. Adopting it
# trades valid content that colours today for the three malformed shapes below.
#
# Run the tool both ways before reopening this. The ratio decides it, the ratio answers to
# TiddlyWiki's tree rather than to anything written here, and the cut reading answers for the
# input a guard would actually meet.

<$link to=<<>>>go</$link>
# ^^^^ - entity.name.tag.html.tiddlywiki5

<$link a/b="x">go</$link>
# ^^^^ - entity.name.tag.html.tiddlywiki5

<$link to: "x">go</$link>
# ^^^^ - entity.name.tag.html.tiddlywiki5
