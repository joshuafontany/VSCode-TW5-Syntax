# Where TiddlyWiki builds a construct and the grammar answers with nothing of its own.
#
# tools/darkness-witness.js reads every wikitext carrier under both readers and judges each line
# of each construct the host builds. A line here names darkness already looked at; any other dark
# line fails the gate, and a line here that reads lit fails as stale — so a repair retires the debt
# it paid, and a declaration cannot outlive what it explains.
#
#   <carrier>  <rule>  <MISS|LOST>  "<the line, trimmed>"  # reason
#
# MISS  the grammar reads nothing of the line even with the construct standing alone — no rule
# LOST  it reads the construct alone and loses the line in place — text above holds a region
#
# The key carries the line's TEXT rather than its number, so an edit elsewhere in a carrier moves
# nothing here, and an edit to the line itself asks for the judgement again.
#
# OWED marks a divergence standing as debt. A line reading dark on purpose carries its ruling
# instead, and says why a reader should meet plain text where the host builds markup.
#
# `node tools/darkness-witness.js --list` prints every dark line in this form, keys derived.
#
# A reason opening `READER <version>` (tools/reader-scope.js) answers for ONE reader alone — the
# version `tw5-oracle.js` boots against, read off `$tw.version`. A line naming no reader answers to
# whichever reader reads it; one naming a reader neither fails nor reads stale under the other.

# A LIST ITEM AFTER A LINE CLOSING AN HTML BLOCK. TiddlyWiki keeps the list open past `</div> run`
# and opens a fresh item on the next `*`. The grammar has fallen into a paragraph by then, so the
# marker reads as prose — and reads so with the whole list standing alone.
tests/samples/tiddlywiki5.basic.tw  list  MISS  "* Inline <span>"  # OWED — the host opens a fresh item here; the grammar reads a paragraph
tests/samples/tiddlywiki5.basic.tw  list  MISS  "* <$my-widget"  # OWED — the host opens a fresh item here; the grammar reads a paragraph
tests/samples/tiddlywiki5.inline.widgets.tw  list  MISS  "* Inline <span>"  # OWED — the host opens a fresh item here; the grammar reads a paragraph
tests/samples/tiddlywiki5.inline.widgets.tw  list  MISS  "* <$my-widget"  # OWED — the host opens a fresh item here; the grammar reads a paragraph
tests/samples/tiddlywiki5.tw  list  MISS  "* Inline <span>"  # OWED — the host opens a fresh item here; the grammar reads a paragraph
tests/samples/tiddlywiki5.tw  list  MISS  "* <$my-widget"  # OWED — the host opens a fresh item here; the grammar reads a paragraph

# A WIDGET'S ATTRIBUTES ACROSS LINES. The widget opens at line 81 and TiddlyWiki reads every line
# below as its own — backtick-substituted values spanning lines among them. The grammar reads none
# of these lines, even with the widget standing alone.
tests/samples/tiddlywiki5.inline.widgets.tw  html  MISS  "string-replaced-attribute=`"  # OWED — a backtick-substituted value opening across lines
tests/samples/tiddlywiki5.inline.widgets.tw  html  MISS  "`"  # OWED — the backtick closing that value on a later line
tests/samples/tiddlywiki5.inline.widgets.tw  html  MISS  "string-replaced-block=```"  # OWED — a triple-backtick value opening across lines
tests/samples/tiddlywiki5.inline.widgets.tw  html  MISS  "paragaph continues, then ends"  # OWED — the interior of that value
tests/samples/tiddlywiki5.inline.widgets.tw  html  MISS  "```"  # OWED — the triple backtick closing it
tests/samples/tiddlywiki5.inline.widgets.tw  html  MISS  "fitler=\"[filter[test]]\" #broken"  # OWED — an attribute the host still reads as the widget's own
corpus/wikitext/html.multiline-tags.tw  html  MISS  "name=\"targetTiddler\""  # OWED — an attribute on a later line of a multi-line tag

# ITALIC ACROSS LINES. TiddlyWiki carries italic across a line inside one block. The grammar's
# italic opens only where its close stands on the same line — `//(?=[^\n]*//)` — the bound that
# keeps an unclosed `//` from running past its own line.
corpus/wikitext/inline.emphasis.tw  italic  MISS  "//italic across"  # OWED — italic the host carries across a line
corpus/wikitext/inline.emphasis.tw  italic  MISS  "two lines//"  # OWED — italic the host carries across a line

# CONSTRUCTS A REGION ABOVE HOLDS. Each reads as itself alone and as something else in place.
corpus/wikitext/degenerate.unterminated.tw  html  LOST  "<div class=\"never-closed\">"  # READER 5.5.0-prerelease OWED — the host builds this element; a region opened above it holds the grammar here
corpus/wikitext/inline.links.tw  extlink  LOST  "which is the reading a reader sees: [ext[https://example.com/unclosed"  # OWED — the host builds this external link; the grammar reads it alone and loses it in place
tests/samples/tiddlywiki5.lists.tw  codeinline  LOST  "* `raw``raw`not raw"  # OWED — the host builds two code runs side by side; the grammar reads each alone and loses them together
tests/samples/tiddlywiki5.tw  codeinline  LOST  "* `raw``raw`not raw"  # OWED — the host builds two code runs side by side; the grammar reads each alone and loses them together

# WHITESPACE BEFORE A BLOCK. TiddlyWiki skips leading whitespace, tabs included, before a block rule,
# so `!!!` behind tabs still opens a heading. Read alone behind spaces, the grammar reads the heading.
corpus/wikitext/blocks.code-fences.tw  codeinline  MISS  "```"  # READER 5.4.1 — 5.4.1's codeblock rule does not vary its fence length across a nested run of backtick fences; a shorter closing run inside a longer nesting reads back as inline code rather than closing the block, where the fork's rule tracks the nesting and closes correctly.
corpus/wikitext/degenerate.unterminated.tw  bold  MISS  "An unterminated ''bold run that reaches the end of its block."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
corpus/wikitext/degenerate.unterminated.tw  italic  MISS  "An unterminated //italic run reaching the end of its block."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
corpus/wikitext/degenerate.unterminated.tw  strikethrough  MISS  "An unterminated ~~strikethrough run reaching the end of its block."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
corpus/wikitext/degenerate.unterminated.tw  underscore  MISS  "An unterminated __underscore run reaching the end of its block."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
corpus/wikitext/degenerate.unterminated.tw  superscript  MISS  "An unterminated ^^superscript run reaching the end of its block."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
corpus/wikitext/degenerate.unterminated.tw  subscript  MISS  "An unterminated ,,subscript run reaching the end of its block."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
corpus/wikitext/degenerate.unterminated.tw  codeinline  MISS  "An unterminated `code span in prose."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
corpus/wikitext/inline.emphasis.tw  bold  MISS  "A lone '' mark that never closes."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
corpus/wikitext/inline.emphasis.tw  underscore  MISS  "Prose naming __localVar in passing, and nothing after it should read underlined."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
corpus/wikitext/inline.emphasis.tw  bold  MISS  "''bold with no closer at all"  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
tests/samples/canary-control.tw  underscore  MISS  "Prose naming __localVar in passing."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
tests/samples/canary-control.tw  bold  MISS  "Prose with ''an unclosed bold run."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
tests/samples/canary-control.tw  italic  MISS  "Prose with //an unclosed italic run."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
tests/samples/canary-control.tw  codeinline  MISS  "Prose with `an unclosed backtick."  # READER 5.4.1 — this inline run opens with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the tiddler to recover (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), while the fork refuses the same run and leaves it as text. The grammar reads the fork's refusal, so it agrees with the fork and diverges from 5.4.1 alone.
tests/samples/tiddlywiki5.basic.tw  superscript  MISS  "|^^^valign=top|,,,valign=bottom|"  # READER 5.4.1 — the `,,,valign=bottom` row above opens a subscript run that 5.4.1 recovers by scanning to the end of the tiddler (the same `unterminated-*` ceiling), so every construct below sits inside ONE subscript node's plain text under 5.4.1, where the fork closes the run nearby and reads each construct on its own; the line reads dark because 5.4.1 never opens it as its own construct at all.
tests/samples/tiddlywiki5.basic.tw  html  LOST  "translates to `<br>` in HTML.\"\"\" And"  # READER 5.4.1 — the `,,,valign=bottom` row above opens a subscript run that 5.4.1 recovers by scanning to the end of the tiddler (the same `unterminated-*` ceiling), so every construct below sits inside ONE subscript node's plain text under 5.4.1, where the fork closes the run nearby and reads each construct on its own; the line reads dark because 5.4.1 never opens it as its own construct at all.
tests/samples/tiddlywiki5.tw  superscript  MISS  "|^^^valign=top|,,,valign=bottom|"  # READER 5.4.1 — the `,,,valign=bottom` row above opens a subscript run that 5.4.1 recovers by scanning to the end of the tiddler (the same `unterminated-*` ceiling), so every construct below sits inside ONE subscript node's plain text under 5.4.1, where the fork closes the run nearby and reads each construct on its own; the line reads dark because 5.4.1 never opens it as its own construct at all.
tests/samples/tiddlywiki5.tw  html  LOST  "translates to `<br>` in HTML.\"\"\" And"  # READER 5.4.1 — the `,,,valign=bottom` row above opens a subscript run that 5.4.1 recovers by scanning to the end of the tiddler (the same `unterminated-*` ceiling), so every construct below sits inside ONE subscript node's plain text under 5.4.1, where the fork closes the run nearby and reads each construct on its own; the line reads dark because 5.4.1 never opens it as its own construct at all.
