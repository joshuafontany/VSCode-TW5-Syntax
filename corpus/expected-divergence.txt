# Divergences that stand by ruling.
#
# Each line names a scope prefix, optionally scoped to one fixture, and the reason it stands.
# A line carrying no reason names no ruling. Every span these explain stands where TiddlyWiki
# refuses and the grammar reads on anyway, deliberately.
#
# A reason opening `READER <version>` (tools/reader-scope.js) answers for ONE reader alone — the
# same reason-prefix flag swallow-witness.js already carries for HOST, generalized to the axis
# TW5_PATH moves. A line naming no reader answers to whichever reader reads it.

meta.link.wikilink                            # TiddlyWiki ships CamelCase linking disabled; the scope stands quiet so a wiki that enables it can colour it back
meta.tag.metadata.doctype                     # a .tid may hold a whole TiddlyWiki HTML document, where a doctype reads; wikitext builds no node for one
meta.tag.metadata.processing.xml              # the same reading, for an XML declaration
html.basic.tw:punctuation.definition.tag       # the fixture holds a whole HTML document, whose DOCTYPE wikitext builds no node for — the two metadata families above name the same reading
html.basic.tw:entity.name.tag                 # the same fixture
html.basic.tw:entity.other.attribute-name     # the same fixture
test.html.tw:punctuation.definition.tag       # the same reading, in the second fixture holding an HTML document
test.html.tw:entity.name.tag                  # the same fixture
test.html.tw:entity.other.attribute-name      # the same fixture
metadata.tags.tw:punctuation.definition.tag   # the fixture exists to exercise the three metadata families above
metadata.tags.tw:entity.name.tag              # the same fixture
metadata.tags.tw:entity.other.attribute-name  # the same fixture
metadata.tags.tw:punctuation.separator.key-value # the same fixture
metadata.tags.tw:string.quoted                # the same fixture
metadata.tags.tw:keyword.control.shortcut     # the same fixture
metadata.tags.tw:meta.shortcut                # the same fixture
pragmas.tw:punctuation.definition.directive   # a \rules run above narrows the active rule set, and no TextMate grammar can follow it
pragmas.tw:keyword.control.directive          # the same \rules run
pragmas.tw:meta.directive                     # the same \rules run
pragmas.tw:keyword.operator                   # the argument of a pragma the same \rules run removed
degenerate.illegal.tw:                        # a fixture written to be malformed
degenerate.unterminated.tw:                   # a fixture written to leave constructs open
degenerate.unterminated-attribute.tw:         # a fixture written to leave an attribute value open
degenerate.unterminated-definition.tw:        # a fixture written to leave a definition open

# The dialect. memetic-wikitext+tiddlywiki includes the whole base grammar and adds a
# vocabulary of its own — carriers, sigils, lar: addresses, block checks — which TiddlyWiki's
# parser does not read and never will. Every scope carrying the dialect's suffix diverges by
# construction; every scope it INHERITS keeps the base suffix and answers like any other.
*.memetic-wikitext                            # the dialect's own vocabulary, unread by the host parser

# Divergences on TiddlyWiki's own tiddlers, each traced to its cause.

markup.other.preformatted.hardlinebreaks  # TiddlyWiki builds no container node for a hardlinebreaks block — its text nodes and line breaks are all it emits — so a scope over the block's content stands over text by construction
RangeWidget Example.tid:meta.transclusion  # READER 5.5.0-prerelease — a transclusion opening at the end of a widget tag that continues onto the next line

# ── A STATE TOGGLE NEVER TAKES A COLOUR AWAY ─────────────────────────────────
#
# TiddlyWiki carries pragmas that change what its parser reads: `\rules except rules` turns the
# rules pragma off, `\rules only table` leaves one rule standing, `\parsermode inline` reads the
# rest of the tiddler as one inline run. After such a line the parser refuses constructs it would
# otherwise build, and it refuses them for the rest of the tiddler.
#
# This grammar colours them anyway, and the ruling stands on what colouring DOES. At design time a
# reader wants to see the base language: that a line IS a rules pragma, that a run IS bold, whatever
# a toggle further up has switched off. The operator holds the tiller for render time — they wrote
# the toggle, they know what it does, and they can read its effect from the toggle itself. A grammar
# that greyed out everything below a `\rules except` line would hide the language from the person
# editing it, to describe a state they authored.
#
# So a construct the grammar reads and a toggle disabled stands here, and the divergence gate reads
# it as a ruling rather than as overreach.
meta.directive.rules                          # the rules pragma keeps its colour after a toggle that disabled it — a reader edits the base language, and the toggle above says what the renderer will do
keyword.control.directive.rules               # the same reading, for the directive's own keyword
keyword.operator.only                         # the same reading, for the only operator inside one
keyword.operator.except                       # the same reading, for the except operator inside one
tiddlywiki5.tw:punctuation.definition.directive  # the same reading, for the backslash opening a rules pragma the fixture disabled above
tiddlywiki5.tw:variable.parameter                # the same reading, for the rule names such a pragma lists

# ── THE STRUCTURAL PATTERN CARRIES THE COLOUR ────────────────────────────────
#
# A shortcut reads `<% keyword ... %>`, and the keywords this TiddlyWiki ships come from one rule
# module — conditional.js wants `if`, and the family closes on `endif`. A wiki can carry more: a
# tiddler or a plugin registers a wikirule of its own, and the shortcut a reader writes tomorrow
# stands unknown to the parser booted here today.
#
# So the grammar colours the STRUCTURE — the `<%`, the `%>`, the keyword between them — and names an
# unrecognised keyword as unrecognised rather than refusing the shape. A reader looking at
# `<% iff %>` learns that they wrote something shortcut-shaped and that this wiki does not answer to
# it; a reader looking at a shortcut their own plugin defines sees it read as one.
#
# `meta.link.wikilink` above stands on the same reading, for a rule TiddlyWiki ships and disables.
# This names the other direction: a rule a wiki may add.
#
# The cost stands measured and stands accepted: prose that NAMES the syntax — "delimited with <% and
# %>" — colours as a shortcut across the words between. A sentence about the language reads as the
# language.
meta.shortcut                                 # a wiki or a plugin may register a shortcut this TiddlyWiki never shipped, so the structural pattern carries the colour and the keyword names itself
keyword.control.shortcut.unrecognized         # the reading that says so: shortcut-shaped, and no rule here answers to this keyword
punctuation.definition.tag.shortcut           # the marks that open and close one
newsyntax.tw:punctuation.definition.tag       # a sentence naming the syntax carries the marks it names
tiddlywiki5.basic.tw:punctuation.definition.tag  # the same sentence, in the fixture that quotes it
tiddlywiki5.tw:punctuation.definition.tag        # and again

# ── THE MARK CARRIES THE COLOUR, WHATEVER THE PARSER MAKES OF IT ─────────────
#
# The same reading, in two fixtures that exercise degenerate forms on purpose. Each ruling names ONE
# fixture, so the shape still reports anywhere else and this gate keeps its teeth.
#
# THREE STYLE OPENERS IN A ROW, AND WHAT STANDS ABOVE THEM. `@@color:blue;`,
# `@@background-color:red;`, `@@.class1.class2` on consecutive lines: this grammar reads three block
# openers, and so, on its own, does TiddlyWiki — it accumulates all three into ONE element carrying
# style="color:blue;background-color:red;" class="class1 class2". Measured on those three lines
# alone, the class names read as BUILT and nothing diverges.
#
# What diverges comes from ABOVE them. The fixture opens with a bare `@@` pair — an empty style block
# — and after it the parser reads the three openers as a paragraph: `color:blue;` arrives as plain
# text, `background-color:red;` as an inline style run, `.class1.class2` as plain text again. The
# same three lines, one empty block earlier in the tiddler, and the marks mean something else.
#
# No rule in this grammar can see that. A TextMate rule fires on a line without knowing which block
# boundary a construct above it consumed, and the reading it would need — did an empty style block
# eat the boundary — stands in the parser's state rather than in the text. So the marks carry the
# colour, and the author reads what they wrote.
#
# AN IMAGE WITH A BLANK SOURCE. `[img[ ]]` carries every mark an image carries and no source between
# them. TiddlyWiki refuses it; the marks still say what the author reached for, and a reader looking
# at a coloured image shape with nothing inside it can see what the shape wants.
#
# The source may cross a line, and both readers follow it: `[img[pic` on one line and `.png]]` on the
# next builds an image node upstream and colours as one here, tooltip form included. Measured, so a
# reader wondering whether the mark stops at the line end can stop wondering. What TiddlyWiki refuses
# stands narrower than that — a source that carries nothing at all.
tiddlywiki5.basic.tw:punctuation.definition.style.markup      # an inline style opener with no closer before its paragraph ends: TiddlyWiki keeps the marks as text (`unterminated-styleinline`), and the grammar opens the run on its marks before it can see whether a closer stands
tiddlywiki5.basic.tw:markup.bold                              # READER 5.5.0-prerelease — a bold run on the line after the closing mark, which the parser reads as content of the run above it
tiddlywiki5.basic.tw:punctuation.definition.markup.begin.bold # the marks opening that run
tiddlywiki5.basic.tw:punctuation.definition.markup.end.bold   # and closing it
tiddlywiki5.tw:punctuation.definition.style.markup            # the same unclosed inline opener, in the fixture that carries every construct
tiddlywiki5.tw:markup.bold                                    # READER 5.5.0-prerelease — the same reading
tiddlywiki5.tw:punctuation.definition.markup.begin.bold       # the same reading
tiddlywiki5.tw:punctuation.definition.markup.end.bold         # the same reading
definitions.closer-name.tw:  # EVERY SPAN in this carrier answers to its one closer. `\end` carrying a name that fails to match closes nothing in TiddlyWiki — the definition runs to the end of the source with a BLANK body and an `unterminated-definition` diagnostic. The grammar closes on the closer it can see. The deciding evidence sits on that line, so this stands OWED rather than structural: matching it needs a backreference to the name, which an end pattern can only take from its OWN begin, and the name stands captured one rule out.  # MEASURED AND DECLINED: a backreference in an end pattern DOES reach the name its own begin captured — probed against vscode-textmate, a matching name closes, a bare closer closes, and a name that fails to match closes nothing, which is the host's own reading exactly. The cost sits in the SECOND engine: Shiki translates every pattern to JavaScript and refuses `\3` outright — `Not enough capturing groups defined to the left` — because a standalone compile cannot see the begin's captures. A reader meeting this grammar on a documentation site would lose every definition's block reading to fix a closer nobody writes on purpose, and that reader has no other advocate here. The closer this grammar accepts and the host refuses costs one authoring error; the fix costs every definition in the other engine.

# A NARROWED RULE SET CHANGES WHAT EVERY LINE BELOW IT MEANS, and a grammar cannot follow it.
# `\rules only <names>` keeps the rules named and `\rules except <names>` drops them, both by editing the
# parser's own rule arrays — and a rule stack carries REGIONS, never a rule table, so every construct below
# a narrowing keeps the colour its markup earns while the host reads flat text. Three carriers hold the three
# readings, and each span below stands where the grammar paints a construct the narrowing retired.
pragmas.rules-except.tw:markup.bold                                    # the carrier stands `\rules except bold`, so the host drops the bold rule alone and reads this run as text — the same ceiling, narrowed the other way
pragmas.rules-except.tw:punctuation.definition.markup.begin.bold       # the carrier stands `\rules except bold`, so the host drops the bold rule alone and reads this run as text — the same ceiling, narrowed the other way
pragmas.rules-except.tw:punctuation.definition.markup.end.bold         # the carrier stands `\rules except bold`, so the host drops the bold rule alone and reads this run as text — the same ceiling, narrowed the other way
pragmas.rules-none.tw:markup.bold                                      # the carrier stands `\rules only` with no names, so the host keeps NO rule and reads the whole file as one paragraph — the widest reading of the same ceiling
pragmas.rules-none.tw:markup.raw.inline                                # the carrier stands `\rules only` with no names, so the host keeps NO rule and reads the whole file as one paragraph — the widest reading of the same ceiling
pragmas.rules-none.tw:punctuation.definition.link.begin                # the carrier stands `\rules only` with no names, so the host keeps NO rule and reads the whole file as one paragraph — the widest reading of the same ceiling
pragmas.rules-none.tw:punctuation.definition.link.end                  # the carrier stands `\rules only` with no names, so the host keeps NO rule and reads the whole file as one paragraph — the widest reading of the same ceiling
pragmas.rules-none.tw:punctuation.definition.markup.begin.bold         # the carrier stands `\rules only` with no names, so the host keeps NO rule and reads the whole file as one paragraph — the widest reading of the same ceiling
pragmas.rules-none.tw:punctuation.definition.markup.end.bold           # the carrier stands `\rules only` with no names, so the host keeps NO rule and reads the whole file as one paragraph — the widest reading of the same ceiling
pragmas.rules-none.tw:punctuation.definition.raw.markup.single.begin   # the carrier stands `\rules only` with no names, so the host keeps NO rule and reads the whole file as one paragraph — the widest reading of the same ceiling
pragmas.rules-none.tw:punctuation.definition.raw.markup.single.end     # the carrier stands `\rules only` with no names, so the host keeps NO rule and reads the whole file as one paragraph — the widest reading of the same ceiling
pragmas.rules-none.tw:string.other.link.title                          # the carrier stands `\rules only` with no names, so the host keeps NO rule and reads the whole file as one paragraph — the widest reading of the same ceiling
pragmas.rules-only.tw:entity.name.section                              # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this construct as text. A rule stack carries regions, never the parser's own rule table, so no TextMate grammar follows a narrowing
pragmas.rules-only.tw:entity.name.tiddler.field.title                  # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this construct as text. A rule stack carries regions, never the parser's own rule table, so no TextMate grammar follows a narrowing
pragmas.rules-only.tw:markup.heading                                   # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this construct as text. A rule stack carries regions, never the parser's own rule table, so no TextMate grammar follows a narrowing
pragmas.rules-only.tw:markup.list.unnumbered.unordered.ul.li           # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this construct as text. A rule stack carries regions, never the parser's own rule table, so no TextMate grammar follows a narrowing
pragmas.rules-only.tw:markup.raw.inline                                # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this construct as text. A rule stack carries regions, never the parser's own rule table, so no TextMate grammar follows a narrowing
pragmas.rules-only.tw:punctuation.definition.link.begin                # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this construct as text. A rule stack carries regions, never the parser's own rule table, so no TextMate grammar follows a narrowing
pragmas.rules-only.tw:punctuation.definition.link.end                  # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this construct as text. A rule stack carries regions, never the parser's own rule table, so no TextMate grammar follows a narrowing
pragmas.rules-only.tw:punctuation.definition.raw.markup.single.begin   # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this construct as text. A rule stack carries regions, never the parser's own rule table, so no TextMate grammar follows a narrowing
pragmas.rules-only.tw:punctuation.definition.raw.markup.single.end     # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this construct as text. A rule stack carries regions, never the parser's own rule table, so no TextMate grammar follows a narrowing
pragmas.rules-only.tw:punctuation.definition.unnumbered.unordered.ul.li.list.markup # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this construct as text. A rule stack carries regions, never the parser's own rule table, so no TextMate grammar follows a narrowing
pragmas.rules-only.tw:string.other.link.title                          # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this construct as text. A rule stack carries regions, never the parser's own rule table, so no TextMate grammar follows a narrowing
spans.multiline.tw:entity.name.tag.html   # READER 5.5.0-prerelease — an XML declaration inside a typed block: the host parses the block under its declared type and builds no HTML element, where the grammar reads the tag it can see
inline.links.tw:punctuation.definition.image.inner.begin   # a marker whose punctuation name now stands alone: the image and external-link marks carried a `keyword.other` family beside their punctuation, which read as a keyword in 55 of 65 themes where a glyph wants punctuation. The span the host refuses stands exactly where it stood
inline.links.tw:punctuation.definition.image.outer.begin   # a marker whose punctuation name now stands alone: the image and external-link marks carried a `keyword.other` family beside their punctuation, which read as a keyword in 55 of 65 themes where a glyph wants punctuation. The span the host refuses stands exactly where it stood
inline.links.tw:punctuation.definition.link.external.inner.begin   # a marker whose punctuation name now stands alone: the image and external-link marks carried a `keyword.other` family beside their punctuation, which read as a keyword in 55 of 65 themes where a glyph wants punctuation. The span the host refuses stands exactly where it stood
inline.links.tw:punctuation.definition.link.outer.begin   # a marker whose punctuation name now stands alone: the image and external-link marks carried a `keyword.other` family beside their punctuation, which read as a keyword in 55 of 65 themes where a glyph wants punctuation. The span the host refuses stands exactly where it stood
pragmas.rules-only.tw:punctuation.definition.tag.macrocall            # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this call and transclusion as text
pragmas.rules-only.tw:punctuation.definition.tag.transclusion         # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this call and transclusion as text
pragmas.rules-only.tw:entity.name.function.macro                      # the carrier stands `\rules only bold italic`, so the host drops every other rule and reads this call and transclusion as text
spans.multiline.tw:entity.other.attribute-name.html   # READER 5.5.0-prerelease — an XML declaration inside a typed block: the host parses the block under its declared type and builds no HTML element, where the grammar reads the tag it can see
spans.multiline.tw:punctuation.definition.tag.html   # an XML declaration inside a typed block: the host parses the block under its declared type and builds no HTML element, where the grammar reads the tag it can see
spans.multiline.tw:punctuation.separator.key-value.html   # READER 5.5.0-prerelease — an XML declaration inside a typed block: the host parses the block under its declared type and builds no HTML element, where the grammar reads the tag it can see
spans.multiline.tw:string.quoted.double.html   # an XML declaration inside a typed block: the host parses the block under its declared type and builds no HTML element, where the grammar reads the tag it can see
spans.multiline.tw:punctuation.definition.tag.begin.html   # READER 5.4.1 — the `// a line comment` inside the script opens an italic run with no closer ahead; 5.4.1 still BUILDS it, scanning to the end of the block, so the `</script>` that closes the element reads as italic text there, where the fork refuses the run and builds the closing tag the grammar paints
spans.multiline.tw:punctuation.definition.tag.end.html   # READER 5.4.1 — the same unterminated italic run, reaching the `>` of `</script>`

# A DELIMITER OVER KEPT TEXT. overreach-check judges a claim naming one of this grammar's delimiters
# against the TIGHTEST cover, since such a claim asserts the character IS markup; a table, a cell or
# a paragraph around it cannot turn a kept character into a mark. Judged by the widest cover, every
# line below read as agreement.
tiddlywiki5.pragma.tw:punctuation.definition.link   # OWED — a bare `\rules` ends the pragma zone (rules.js:26 needs whitespace after the word), so every definition below reads as prose and an unclosed `<parameter>` from a function body nests the rest of the file; the grammar still reads definitions there
tiddlywiki5.pragma.tw:punctuation.definition.mvv-display   # OWED — the same pragma zone the bare `\rules` above it closes
tiddlywiki5.math.tw:punctuation.definition.tag.begin.html   # an element body TiddlyWiki reads INLINE: html.js:52-71 parses a body as blocks only where the rule ran in block mode or a BLANK LINE follows the tag, and this `<p>` stands inside a paragraph with text on the line after it, so the markup in its body stays text. Whether that blank line follows stands on the next line, which no TextMate pattern reads
tiddlywiki5.math.tw:punctuation.definition.tag.end.html   # the same inline `<p>` body
tiddlywiki5.math.tw:punctuation.definition.list   # the same inline `<p>` body: a definition-list mark inside it stays text
core/templates/html-div-skinny-tiddler.tid:punctuation.definition.tag   # OWED — markup inside a code run TiddlyWiki carries across lines: codeinline closes at the next matching backtick run, newlines and all, while the grammar closes a code run on its own line — the bound that keeps an unclosed backtick from running past it — and reads the lines after as markup. It awaits the ruling italic across lines awaits
es-ES/tiddlers/Formatting_text_in_TiddlyWiki.tid:punctuation.definition.tag   # READER 5.5.0-prerelease OWED — the same code run across lines, holding a whole `\define`
es-ES/tiddlers/Formatting_text_in_TiddlyWiki.tid:punctuation.definition.string   # READER 5.5.0-prerelease OWED — the same code run across lines
es-ES/tiddlers/Formatting_text_in_TiddlyWiki.tid:punctuation.definition.transclusion   # READER 5.5.0-prerelease OWED — the same code run across lines
tw5.com/tiddlers/widgets/ListWidget.tid:punctuation.definition.list   # READER 5.5.0-prerelease OWED — the grammar claims a definition-list mark at a `:` standing mid-line after `<<counter>>`, inside a `<div>` whose body TiddlyWiki reads inline; the host keeps the colon as text

# SURFACED WHEN THE CORPUS JOINED THE GATES. overreach-check ran over corpus/ only by hand while
# gate-report skipped it, so the divergences below stood unreported. Each stands traced; OWED ones
# retire with the fix that pays them.
inline.links.tw:keyword.other.img   # OWED — an `[img[` with no closer builds no image, and the grammar paints the opener's keyword before any closer stands
inline.links.tw:keyword.other.ext   # OWED — an `[ext[` with no closer builds no ext link, and the grammar paints the opener's keyword before any closer stands
html.deprecated.tw:punctuation.definition.tag.begin.html   # OWED — `<div ="x">` and `<div style==>` build no element, and the grammar paints their tags
html.deprecated.tw:punctuation.definition.tag.end.html   # OWED — the same malformed tags the host keeps as text
tiddlywiki5.inline.filteredtransclusions.tw:punctuation.definition.transclusion.filteredtranscludeinline.end   # OWED — filteredtranscludeinline.js's filter group requires at least one character, so the empty `{{{}}}` two lines above never matches where it stands; the host's regex scans on past it and closes on the next `}}` it finds, swallowing every opener after it by one pair until the file re-syncs. The grammar reads each `{{{…}}}` in place, where an author's eye reads them too — the divergence is the host's own quirk on an empty filter, surfaced now that the closer's punctuation stands apart from its style value instead of hiding inside one wide mark
inline.transclusions.tw:punctuation.definition.tag.transclusion   # OWED — an empty `{{{}}}` never matches the filtered rule (its filter needs a character), so the plain `transcludeinline` rule matches the SECOND `{` through the second-to-last `}` instead, leaving the outer `{` and outer `}` as text either side of it. The grammar reads the outer braces as its own filtered-transclusion delimiters, one character wider on each side than the empty construct the host actually builds — surfaced now that the closer's final `}` carries its own mark instead of hiding inside one wide one

tiddlywiki5.basic.tw:punctuation.definition   # READER 5.4.1 — the `,,,valign=bottom` row above (line ~62) opens a subscript run that 5.4.1 recovers by scanning to the end of the tiddler (the `unterminated-*` ceiling tools/recovery-witness.js measures against the host), swallowing every fenced code block, typed block, comment and quoteblock closer after it into ONE subscript node's plain text; the fork closes the run nearby and reads each construct on its own. Every mark the grammar paints in that swallowed reach answers to the fork's reading, not 5.4.1's
tiddlywiki5.tw:punctuation.definition   # READER 5.4.1 — the same reach, the same sample carried a second time
canary-control.tw:punctuation.definition   # READER 5.4.1 — the deliberately unclosed constructs this control carries (an unclosed bold run, an unclosed backtick) recover the same way under 5.4.1, scanning to the end of the tiddler and swallowing the widget tag and syslink after them into inline code; the fork refuses each unclosed run in turn and reads the widget tag on its own
blocks.code-fences.tw:punctuation.definition   # READER 5.4.1 — 5.4.1's codeblock rule does not vary its fence length across a nested run of backtick fences (see corpus/swallow-ledger.txt's `overbound codeinline`); the shorter closer inside the longer nesting reads back as inline code that runs to the next backtick it finds, swallowing every construct between into that one codeinline. The fork's rule tracks the nesting and closes each fence correctly
reading.in-prose.mem:punctuation.definition.heading   # READER 5.4.1 — the `lar://` scheme's own double slash opens an italic run with no closer ahead (the same `unterminated-*` recovery as the samples above); 5.4.1 scans to the end of the tiddler and swallows every heading after it into that run, where the fork refuses the same run and reads each heading on its own

# The pinned 5.4.1 devDependency and this repository's own fork carry IDENTICAL bytes for each of
# the four host tiddlers below (diffed byte for byte), so these four answer to `overreach-host`
# alone: a malformed macro call or list marker already standing in the upstream translation reads
# as an UNTERMINATED construct, and 5.4.1's scan-ahead recovery lands somewhere the fork's does not.
es-ES/tiddlers/AlertMechanism.tid:   # READER 5.4.1 — `<<.tag ~$:/tags/Alert]].` opens a macro call with no `>>` on its own line (the source carries `]]` where a closer belongs, an upstream typo); 5.4.1's macrocall rule scans ahead for the next `>>` and swallows the prose after it as parameters, where the fork's rule recovers at a different point
geospatialdemo/tiddlers/Features.tid:   # READER 5.4.1 — a mismatched HTML closing tag reads as an unterminated element under 5.4.1's html rule, carrying to the next `>` it finds, where the fork's rule recovers at a different point
ja-JP/tiddlers/howtos/KeyboardShortcuts.tid:   # READER 5.4.1 — a description-list marker's own spacing reads differently once 5.4.1's list rule recovers past it, where the fork's rule does not
tw5.com/tiddlers/tag-pill Macro (Examples).tid:   # READER 5.4.1 — a list marker inside an HTML block reads differently once 5.4.1's html rule recovers past it, where the fork's rule does not
tw5.com/tiddlers/widgets/ActionDeleteFieldWidget.tid:   # READER 5.4.1 — a mismatched HTML closing tag reads as an unterminated element under 5.4.1's html rule, carrying to the next `>` it finds, where the fork's rule recovers at a different point
