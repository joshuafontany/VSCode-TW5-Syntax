450

# RE-SEATED, and saying so. A floor counts what this corpus reaches across the scopes a grammar
# DECLARES, so changing the grammar moves the denominator and the count falls while nothing reads
# worse. That happened here: the dialect went to syntaxes/attic/ carrying 33 scopes with it.
#
# Re-seating a gauge after its instrument changes names a different act from loosening one, and the
# two read alike from outside — which buys this paragraph its place. From here the ratchet resumes,
# and rises as the seed grows a vocabulary of its own.

# 2026-09-08: RE-SEATED again, and upward. A call site gained a second scope naming what it is
# rather than what it was defined by, and the published name stands beside it, so the corpus reaches
# ten names it could not reach before. A floor left under a widened vocabulary calls a later LOSS a
# pass, which is the fault this ratchet exists to catch.

# 2026-09-11: RAISED, and by specimens rather than by a vocabulary. Three scopes stood reached and
# unpinned, so a loss of three would have passed as a pass — the one thing a ratchet exists to
# refuse. The carriers that reached them arrived for other questions entirely: `reading.in-prose`
# stands every construct inside a sentence, and the degenerate carriers give each unterminated block
# shape one of its own, because a block that never closes swallows every recovery after it.
#
# A GAIN LEFT UNPINNED READS EXACTLY LIKE A FLOOR HOLDING. Neither hand that made this gain owned
# the file, and each left it for the other.

