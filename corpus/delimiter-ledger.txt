# What a delimiter inherits from the content it bounds, and what this repository ruled about each.
#
# `contentName` names a region's INTERIOR, so the marks that open and close it stand outside the
# content family by construction. `tools/delimiter-inheritance.js` reads every `contentName` in
# `syntaxes/` and keys each by the two-segment family of its content against the families its
# delimiters carry. A shape named here stands looked at; anything else fails the gate.
#
#   <kind> <content family> <- <delimiter families>   # reason
#
# `inert`              the field sits on a `match` rule, where vscode-textmate reads it nowhere.
#                      MEASURED on every run: both arms read identically, against a control that
#                      parts.
# `covering`           both bounds stand zero-width, so the content spans the marks and nothing
#                      parts.
# `inherits`           every family the content carries also stands on a delimiter or on the
#                      region's own name — the additive cure, already written.
# `parts`              the delimiter stands outside what the content carries.
# `unnamed-delimiter`  a begin/end carrying no captures; the mark takes whatever encloses it.
#
# A REASON SAYS WHETHER THE PARTING SERVES A READER OR COSTS ONE, and a reason opening OWED records
# debt rather than accepting it — the gate stays green and prints it. THE TEST IS NOT SYMMETRY. A
# boundary a reader must SEE wants the delimiter held apart; a mark that belongs TO its content
# wants it stacked. `meta.*` carries no colour of its own in a theme, so a `meta.*` content parting
# from its marks costs a reader nothing whichever way it reads.

# ── the declaration that does nothing ───────────────────────────────────────────────────────────

inert string.quoted <- entity.other+keyword.operator+punctuation.definition+string.quoted+support.type+variable.parameter   # OWED a removal. A dynamic bracket parameter `name=[[value]]` spells itself as a `match` and declares a `contentName` beside it. vscode-textmate reads that field on a begin/end region and nowhere else, so the declaration parses, reports nothing and moves no token — measured on every run of this gate, with and without the field, against a control on a begin/end rule where the field DOES move the reading. Capture 4 already names the interior, so removing the field changes no token; it stands as a declaration a later reader would take for a promise

# ── the content covers its own marks ────────────────────────────────────────────────────────────
# A zero-width bound consumes nothing, so `contentName` spans the delimiters too and the parting
# this gate looks for cannot arise. These carry a different hazard, and the TextMate ceiling names
# it: a `string.*` content covering its own brackets puts that string under every construct opened
# inside, with no way to take it off again.

covering entity.other <- keyword.operator+variable.parameter   # a pragma parameter default, `name:"value"` in its four quotings. The bound stands behind the quote, so the default's own name covers the quotes and the parameter name and the colon keep theirs. Nothing parts and nothing wants moving
covering meta.attribute <- (none)   # a filtered, indirect or macro-valued attribute value. The region opens and closes on lookarounds and the inner patterns name every mark, so the outer declaration adds a structural family over the whole run and takes nothing from the marks
covering meta.embedded <- (none)   # a tiddler body whose header declared a type, opened behind the blank line that ends the field block. No mark stands at either bound, so no delimiter exists to inherit anything
covering meta.multids <- (none)   # a `.multids` file's typed body, opened behind the blank line that ends the field block once the header names a `type` — mirrors `covering meta.embedded` above, under its own family because a `.multids` file's body holds many `title: text` lines rather than one tiddler's text. The end stands on `(?!)`, which never matches, so the region reaches the file's own end; no mark stands at either bound
covering string.quoted <- (none)   # a bracketed attribute value, `attr=[[value]]`. The content spans the brackets and an inner match re-names the interior, so the interior carries the string scope twice. Harmless to a theme, which matches a scope rather than counting it; it reads as a redundancy rather than a parting
covering string.template <- (none)   # a substituted attribute value in backticks, its bounds behind the fences. The inner patterns name the fences and the substitution injection reaches the interior. Named `string.template` (2026-09-21, `LEDGER-3.0.0.md`) rather than `text.substituted`, so the base family a theme already paints for a template string reaches it
covering string.template <- entity.other+keyword.operator+support.type+variable.parameter   # the same value written as a named parameter, so the parameter name and the `=` carry their own names and the backticks fall inside the content. Nothing parts
covering variable.call <- (none)   # a macro-valued attribute, `attr=<<call>>`. The content spans the whole call including its angles, and the call's own rules name the parts inside
covering variable.call <- entity.other+keyword.operator+support.type+variable.parameter   # the same call written as a named parameter. The name and the `=` carry their own names; the angles fall inside the content
covering variable.parameter <- keyword.operator+variable.parameter   # a pragma parameter whose default stands in double parens. Same shape as the four quoted defaults, and the same verdict

# ── a parting that serves a reader ──────────────────────────────────────────────────────────────
# A boundary the reader must SEE. Painting the fence as the guest language hides where the guest
# starts and stops, which is the one thing the fence exists to say.

parts meta.embedded <- entity.other   # a fenced code block naming its language. The fence's language word reads as an attribute name and the body reads as that language. A reader needs the seam, so holding the two apart serves them
parts meta.embedded <- entity.other+punctuation.definition   # a typed block, `$$$type`, handing its body to a guest grammar. The `$$$` marks and the type word stay in this grammar's vocabulary while the body leaves it; the parting IS the boundary
parts meta.embedded <- punctuation.definition+source.css+string.quoted   # a `style` attribute handing its value to the CSS grammar. The closing quote carries both its own punctuation name and `source.css` on overlapping captures, so the guest grammar's region closes cleanly on a mark this grammar still names. Serves: the quote reads as a quote and the value reads as CSS A quoted attribute value's own quote characters now carry the string family beside their punctuation name, so an embedded stylesheet or script keeps its quotes inside the string that holds it — the device MagicPython ships on a quote character, twelve sites there.
parts meta.embedded <- punctuation.definition+source.js+string.quoted   # the same device over an event-handler attribute value handed to the JavaScript grammar A quoted attribute value's own quote characters now carry the string family beside their punctuation name, so an embedded stylesheet or script keeps its quotes inside the string that holds it — the device MagicPython ships on a quote character, twelve sites there.
parts markup.inline <- entity.other   # a fenced block whose declared language this extension bundles no grammar for. The body falls back to `markup.inline.raw` and the fence keeps its language word, which is exactly the seam a reader wants
parts markup.inline <- entity.other+punctuation.definition   # a typed block whose declared type reaches no bundled grammar. Same fallback, same seam
parts markup.inline <- keyword.control+punctuation.definition   # a triple-backtick block with no language named. The fence reads as a fence and the body as raw, which serves: a reader tells a fence from its contents
parts text.git-commit <- entity.other   # a fenced block declaring a language whose grammar names a `text.*` root of its own. Same seam as every other fence
parts text.html <- entity.other   # a fenced block declaring wikitext, whose body re-enters this grammar. The fence stays visible above the body
parts text.html <- entity.other+punctuation.definition   # a typed block declaring wikitext. The `$$$` and the type word hold the seam while the body reads as wikitext again
parts variable.name <- punctuation.definition   # a multi-valued variable's name inside an attribute value, against the `((` and `))` that bound it. The parens read as marks and the name between them reads as the variable it names, which is the reading a reader wants and the one the single-line form already carried. A VALUE SPANNING LINES NEEDS THE REGION: a match reads one line and no more, so a value carried across a break painted nothing inside itself. The name between the parens carries no construct of its own, so the region names its interior outright and stands no inner pattern that could reach past the closing marks — the hazard that answers to `#textReference` next door. Measured at the cure: the single-line reading stands byte-identical under the region, 0 of 32 snapshots drifted
parts comment.block <- punctuation.definition   # a `#` comment line in a syntax-test file. The `#` reads as the mark that opened the comment; the text reads as comment. A theme paints `comment` and most paint `punctuation.definition.comment` with it, and the two standing apart lets a reader see where the comment began

# ── a parting a reader never sees ───────────────────────────────────────────────────────────────
# `meta.*` names structure for a machine. No bundled theme paints a bare `meta` rule, so a `meta.*`
# content standing apart from its marks moves no colour either way.

parts meta.attribute <- punctuation.definition   # an image's attribute list against the inner `[` that closes it. The list carries structure and the bracket carries the mark that ends it, so the two part. THE LIST STANDS AS THE REGION'S CONTENT rather than as its opening match. A `begin` carrying the whole list reads one line and no more — `\G` holds only where the previous match ended, and fails at the first line break — which leaves every line after the opener falling through to `markup.other.image`, a reader meeting an attribute name, its `=` and its value painted as undifferentiated image body. Opened zero-width, the region spans the break and the attribute patterns paint each line, which is what TiddlyWiki's own image rule accepts. No bundled theme rules on a bare `meta` scope, so the bracket keeping `punctuation.definition` alone moves no colour either way
parts meta.conditional <- keyword.control   # a conditional shortcut's body against its `<%if%>` keyword. The keyword reads as a keyword and the body as structure, which is what both are
parts meta.styleblock <- meta.styleblock+punctuation.definition   # a style block's body against its closing `@@`. Both sides carry `meta.*`, so nothing a theme paints stands on either
parts meta.variable <- entity.name+keyword.control+punctuation.definition   # a definition body — function, procedure, widget, macro — against its `\define` keyword, its name and its `\end`. The body is a body and the pragma is a pragma; a reader wants them apart
# checks: family support.function rules-on >= 40, quiet <= 1
# checks: family entity.name.function rules-on >= 50, quiet <= 8
parts meta.variable <- entity.name+punctuation.definition+support.function+variable.name   # a call's parameter run against its `<<` and `>>` and the name between them. The angles and the name carry their own families; the run carries structure. The name between the angles reaches FOUR families at once, and each answers a different reader. `support.function.macro` rides beside the published `variable.name.macro` because a macro IS a function the wiki provides, and because `variable` is the most-shipped leave-it-alone selector in the bundled set: 58 of 65 themes reach a `variable.*` name and 22 of those leave the colour exactly where the editor had it, so a call read under `variable` alone reads as prose to a third of readers. `entity.name.function` carries the same reading a widget's name carries, which is what a call IS to a reader — 59 of 65 themes rule on it and 5 of those leave the colour where the editor had it, the loudest ratio any family on this span reaches. The delimiters keep `punctuation.definition` alone, so the marks stay marks while the name between them reads as the thing being called
parts entity.filter <- punctuation.definition   # a filter run's operand against the `[` and `]` that bound it. RULED 2026-09-18: the operator OVERRODE the one-colour ruling this line carried before — a filter run's bounds no longer carry the operator family beside their own name. FILTERS READ AS PARTS: the run bracket is punctuation only, the run prefix (`+ - ~ = :name`) reads `storage.modifier.prefix.*` parted from both the bracket and the operator name, and the operand brackets part by kind (`variable`, `indirect`, `mvv`) instead of carrying entity families. Measured on this tree (construct-legibility: 0 fallen; per-scope colour price against the 65 bundled themes): prefix vs bracket 0→65, prefix vs operator 0→41, bracket vs operator 0→58, variable bracket vs value 20→48, indirect bracket vs value 10→45. The `{{{ }}}` filtered-transclusion container bracket against a plain run bracket, unaffected by this move on either side, stands 34→48 apart under the SAME per-scope colour reading — that pair moved only because the plain run bracket it is compared against dropped `keyword.operator.filter`, not because the container itself changed; reported, not tuned. The operand-kind brackets (`variable`, `indirect`, `mvv`) collapse to one punctuation colour under this ruling — the glyph still differs, and the operator accepted that trade in exchange for the parts above.
parts meta.tiddle <- punctuation.separator+string.value   # a `.multids` typed tiddler's title against the colon that ends it and the guest language its text embeds, ruled 2026-09-18 beside the `.multids` type-routing entry above. The title reads as the title it is and the separator as the mark it is, while the text after it reads as the guest grammar the header's `type` names — a begin/end block carries the embed, the shape `tw5-tid-file.json` already uses, because a `match` rule's captured `patterns` never resolves a cross-grammar `include` reliably on this engine. Serves: the title stays a title and the embedded language reads as itself, exactly the seam `parts meta.embedded <- entity.other` above draws for a fenced block's language word

# ── a mark that wears its content ───────────────────────────────────────────────────────────────
# A mark that belongs TO its content, where stacking the content's family onto the mark COSTS NO
# READER. Each capture carries its published punctuation name FIRST and the content's families
# LAST, space-separated, the device VS Code's TypeScript grammar ships on its template backticks
# and MagicPython ships twelve times. The content scope stands innermost, so a theme's rendering
# reaches the marks and the punctuation name still reaches every rule it reached before.
#
# MEASURED AT THE CURE, against a control arm stacking nothing: `construct-legibility` reads 820
# pairs over 65 themes and NONE of these three moved one. Each content family here reaches a rule
# in 2 of the 65 bundled themes, so in 63 the marks keep the punctuation colour they carried and in
# 2 they join their run. The cure adds in every theme and takes nothing in any.

inherits markup.subscript <- markup.subscript+punctuation.definition   # the `,,` marks of a subscript run. Two themes lower the run and now lower its marks with it; the other sixty-three paint the marks as punctuation, exactly as before
inherits markup.superscript <- markup.superscript+punctuation.definition   # the `^^` marks of a superscript run. Same two themes, same reading, and the same zero cost across all 820 pairs
inherits markup.other <- markup.other+punctuation.definition   # the `"""` marks of a hard-linebreak run, `markup.other.preformatted.hardlinebreaks`. The marks read as the preformatted run they fence, and no pair moved

# ── a cure that costs a reader, DECLINED ────────────────────────────────────────────────────────
# THE SAME DEVICE, MEASURED, AND IT TAKES MORE THAN IT GIVES. Stacking an emphasis run's family onto
# its own marks makes the whole construct read in one ink, and a construct reading in one ink stops
# parting from a NEIGHBOUR that reads in that same ink. `corpus/legibility-floor.txt` rules that a
# count may rise and may never fall; each cure below drops one, so each stands declined with the
# number that declined it. Measured one family at a time, against a control arm stacking nothing
# and reading zero.
#
# The two deepest falls share one cause, and it sits OUTSIDE these rules: this grammar names a link
# `markup.underline.link.*`, so a theme ruling on `markup.underline` paints links and underline runs
# alike. Curing the mark widens the underline family over the whole run and the collision surfaces.
# `MIGRATION.md` already moved `markup.underline.link.wikilink` to `meta.link.wikilink` for that
# reason; the syslink, image and external-link twins still carry it. THESE FOUR WANT THAT NAME
# MOVED FIRST, and the cure re-measured after.

parts markup.bold <- punctuation.definition   # RULED: the marks stay outside. A theme's bold weight stopping at the text costs a reader nothing a reader notices, and three pairs falling costs one something measurable. Gaining a weight already carried by the letters it wraps buys the least of any cure this gate has weighed. Measured: The `''` marks stand outside `markup.bold`, so a theme's bold weight stops at the text. Stacking `markup.bold` onto them gains that weight in 59 of 65 themes and COSTS three pairs: bold against a bare external link 65 -> 64, against a heading 63 -> 61, against a horizontal rule 63 -> 61. A heading and a rule already read bold in those themes, so the marks joining the run erases the seam
parts markup.italic <- punctuation.definition   # RULED: the marks stay outside, for the reason bold's do — the slant a reader wants sits on the letters, and three pairs pay for putting it on the marks as well. Measured: The `//` marks stand outside `markup.italic`. Stacking gains the slant in 59 of 65 and COSTS three pairs: italic against a quote block 59 -> 57, against a CamelCase link 62 -> 61, against a suppressed link 58 -> 57
parts markup.underline <- punctuation.definition   # RULED: the marks stay outside, and here the cure cannot be had at any price. Two rules wear this family — the `__` run and the `~~` strikethrough — and `markup.underline.link` names every link besides, a spelling markdown shares. Widening either collides with the other BY CONSTRUCTION, and no repair parts them without one abandoning a name its own ecosystem rules on. A CURE BUYING NOTHING STILL COSTS SOMETHING. Measured: Two rules wear this — the `__` underline run and the `~~` strikethrough run, whose content names both `markup.underline.strikethrough` and `markup.strikethrough`. Stacking COSTS four pairs and the worst fall this gate has read: a system link against underline 46 -> 28, a bare external link against underline 65 -> 62, a system link against strikethrough 50 -> 41, a bare external link against strikethrough 65 -> 62. A system link wears `markup.underline.link.syslink`, so widening the underline family over a whole run collides with it head-on
parts comment.strikethrough <- punctuation.definition   # RULED 2026-09-20: the marks stay outside, and the strikethrough run's own marks answer to their construct rather than to the ink its content borrows. `~~x~~` names its content `comment.strikethrough.tiddlywiki5 markup.strikethrough.tiddlywiki5` — the honest name last, deciding the font style, and the comment family ahead of it reaching the themes that rule on neither. Its `~~` marks carry `punctuation.definition.markup.begin|end.strikethrough.tiddlywiki5` and inherit nothing: a mark wearing a comment's ink would read as a comment's own delimiter, which no reader means by `~~`. Measured over the 65 bundled themes: content parts from its marks in 64, and the marks part from a comment's own `punctuation.definition.comment` in 64.
parts entity.name <- markup.heading+punctuation.definition   # a heading's `!` marks against the section name they introduce. The marks carry `punctuation.definition.heading.tiddlywiki5 markup.heading.tiddlywiki5` — a theme's heading rule reaches them in 55 of 65 and a punctuation rule in 42, the same reach the nested spelling carried, and zero pairs moved. The content names the SECTION rather than the heading markup, and no mark is a kind of section name, so the marks take the heading family and the parting from `entity.name` stands

# ── a bound that names no mark ──────────────────────────────────────────────────────────────────
# A begin or end matching a line start, a file start or a blank line consumes no mark, so no
# delimiter stands to inherit anything. These read as bounds rather than as brackets.

unnamed-delimiter markup.other <- (none)   # a style block opening on a lookahead at `@@` and closing on the line carrying the closing `@@`. The marks belong to the sibling rules that name them; this region only holds the span
unnamed-delimiter meta.multids <- (none)   # a `.multids` file's field block and its body, bounded by the blank line between them. A blank line is not a mark
unnamed-delimiter meta.quote <- (none)   # a quote block's body and its top cite, bounded by the enclosing `<<<` run's own lookaheads. The `<<<` marks carry `punctuation.definition.markup.quote.quoteblock.begin` from the rule that opened the block, so the mark reads as a mark and this region adds structure over the span
unnamed-delimiter meta.text <- (none)   # a `.tid` file's field block and its body, bounded by the blank line between them, and by the `type:` line that hands the body to a guest grammar. No mark stands at either bound
unnamed-delimiter text.html <- (none)   # a syntax-test file's wikitext body, opening at the start of the file and never closing. A file has no opening bracket

# 2026-09-12: THE THREE OWED RULINGS STAND WRITTEN INTO THEIR OWN ENTRIES, and the floor falls 4 -> 1.
#
# A HEADER RULING AND AN ENTRY SAYING `OWED` CONTRADICT EACH OTHER, and a floor that can never fall
# guards nothing. The note below stood here while three entries still read OWED A RULING and the gate
# held at four forever. Each entry now carries its verdict beside the measurement that decided it; the
# one debt left is a REMOVAL, not a ruling, and the floor names exactly that one.
#
# 2026-09-11: THE FOUR OWED CURES STAND RULED RATHER THAN OWED.
#
# `markup.underline` names TWO unrelated things in this grammar: the underline emphasis run, and the
# link family — `markup.underline.link.{external,image,syslink}`, which markdown spells the same way.
# Both spellings stand conventional, so widening either collides with the other by construction, and
# no repair parts them without one leaving a name its own ecosystem rules on.
#
# Measured against that collision: a pretty link's BOUNDS carrying the link title family fell 6 pairs
# and cost a wikilink SEVENTEEN themes of distinction from a bare external link. Its SEPARATOR alone
# cost 6 theme-readings and bought no colour whatsoever, the bounds staying punctuation either way.
#
# A CURE BUYING NOTHING STILL COSTS SOMETHING. These four close as rulings.

#
# A TRANSCLUSION'S MARKER CANNOT READ AS ONE OBJECT WITH ITS OWN INTERIOR WITHOUT JOINING THE STRING
# FAMILY, and that purchase costs four declared distinctions. `{{` and `}}` carry the interior's
# published entity name already, and a reader still meets two colours in 27 of 65 bundled themes,
# because the interior's stack reaches `string.text-reference` where the marker's does not. Measured
# both ways: adding `string.text-reference` to the markers lifts one-colour-with-interior from 38 of
# 65 to 65 of 65 with no theme reading either as prose — and drops the transclusion below the
# legibility floor of 64 against a bare external link (54), a code span (57), an entity (62) and a
# heading (63), every one of them a string-family construct. Mirroring the interior's `variable.
# other.reference` instead costs nothing and buys nothing: unity moves 38 to 39.
#
# THE SPACE OF MOVES STANDS CLOSED BY DERIVATION, not by taste. To paint alike, the marker must match the
# rule its interior's stack wins on, so the interior's own scopes are every move available — five of them
# the marker lacks. Measured over all five: `meta.transclusion.tokenize`, `meta.transclusion.text-reference`
# and `meta.support.text-reference` leave unity exactly where it stood at 38 of 65; `variable.other.reference`
# moves it to 39; `string.text-reference` reaches 65 and takes the four distinctions with it.
#
# So the marker keeps the entity name alone. The unity a reader sees in Gruvbox comes from that name
# reaching both marker and interior there, and the 27 themes where it parts pay for four distinctions
# holding everywhere.
parts meta.filter <- punctuation.definition   # a filtered attribute value: the `{{{` and `}}}` carry structure and the filter between them carries its own name, so the two part. A value spanning lines needs the region — a match reads one line and no more, and such a value painted nothing inside itself before
parts meta.transclusion <- punctuation.definition   # a filtered transclusion shorthand's tooltip: filteredtranscludeinline.js splits on the first bare `|`, which closes the filter and opens the tooltip run to `||` or `}}`. The `|` carries `punctuation.definition.transclusion.filteredtranscludeinline.tooltip.tiddlywiki5` and the tooltip text carries `meta.transclusion.filteredtranscludeinline.tooltip.tiddlywiki5 string.entity.attribute.tooltip.tiddlywiki5` — the separator names the construct it belongs to, not the tooltip's own reading, mirroring how the `{{{`/`}}}` marks already part from `meta.filter` above
