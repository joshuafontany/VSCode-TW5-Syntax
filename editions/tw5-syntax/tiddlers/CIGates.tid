title: $:/tw5-syntax/CIGates
type: application/json
tags: $:/tags/TW5Syntax/GrammarData
caption: CI gates
description: Which gates continuous integration runs, and the reason for each one it does not

{
    "alsoGates": [
        "tests-known-gaps",
        "edition:check"
    ],
    "reporting": [
        "signals:check",
        "overreach-host",
        "overreach-cut",
        "upstream-coverage"
    ],
    "skipped": [
        {
            "gate": "overreach",
            "why": "153 spans stand where the grammar claims a construct and TiddlyWiki reads prose, down from 161 since the pragma zone landed. The remaining spans sit inside constructs the parser builds as ONE node while the grammar names their parts \u2014 a directive\u2019s backslash, a parameter name, a shortcut\u2019s angle. The gate counts a part claimed inside a whole the parser built, which reads as a divergence and stands as sub-tokenization. Deciding that reading is the work this gate owes."
        }
    ]
}
